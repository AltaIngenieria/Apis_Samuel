info:
  name: Pais-Dpto
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/Generales/departamentos"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
