info:
  name: Otros Conceptos
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/v1/Generales/ciudades/76"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
