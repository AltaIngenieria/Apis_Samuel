info:
  name: ConsultarAliadoParametrizacion
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v1/Aliados/ConsultarAliadoId/184"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
