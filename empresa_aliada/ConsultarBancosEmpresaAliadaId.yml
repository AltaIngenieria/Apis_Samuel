info:
  name: ConsultarBancosEmpresaAliadaId
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/Bancos/ConsultarBancosEmpresaAliadaId/495"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
